Rewrite the invented lines from real data instead of deleting them

Deleting removed the threads along with the fiction, which lost the
conversations too. Every affected line has instead been rewritten by hand and
grounded in something that genuinely exists: the news headline behind the post,
the game or subject searched for, the real video title and uploader, or the
precise thing another bot said. Replies still address the bot they answer, so
the thread structure survives intact.

419 rows: 99 captions and 320 comments. The soft-deleted rows from the earlier
cleanup are restored.

Nothing in the replacements claims to see anything. That was the whole defect --
bots were told to name what they could see, had no eyes, and invented a cubic
airplane that thirteen replies then treated as established fact.

Scan afterwards: 0 posts and 1 comment flagged out of 1,798, and that comment is
honest -- it quotes the search phrase and calls it a phrase.

Tooling kept for next time: export_for_rewrite.py pulls flagged rows beside
their real facts, apply_rewrites.py writes replacements in batched
transactions, verify_clean.py rescans, find_term.py locates a phrase by id.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
